#!/usr/bin/env bash

# ***** GIT-SVN-PUMP configuration file *****

# GIT_ROOT_URL should point to the source git repository (write access required for marker tags)
#     example:
#     GIT_ROOT_URL=some_git_user@git_server_host:my_project_repo

# SVN_REPO should point to the target svn repository.
#     example:
#     SVN_REPO=https://svn.mycompany.com:18080/svn/my_project_repo

# Optional proxy settings for SVN connection (include none or both!)
#SVN_PROXY_HOST=proxy.mycompany.com
#SVN_PROXY_PORT=8080

# Required SVN variables
#SVN_USER=foo
#SVN_PASSWORD=bar

# ***** Overridable default settings *****

# regex patterns to identify git branches that should be pumped to svn
#PUMPED_BRANCH_PATTERN1='^\s*v[0-9]\.[0-9]\.[0-9]_candidate$'
#PUMPED_BRANCH_PATTERN2='^\s*svn_'

# regex pattern to skip some of the git branches matched by PUMPED_BRANCH_PATTERN1
#EXCLUDED_BRANCHES_PATTERN1='^\s*v0\.0\.0_candidate'

# prefix for git tags generated by git-svn-pump
#TAG_PREFIX=svnrev_
